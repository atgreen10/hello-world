#include <stdio.h>
#include <cs50.h>
#include <math.h>

int main(void)
{
    //variables
    // long long int amex;
    // long long int MC;
    // long long int Visa;
    // long long int invalid;
    long long int cc;
    long long int cctemp1;
    int count;
    int a;

     // prompt
    do
    {
        cc = get_long_long("Enter Credit Card Number ");
    }
    while (cc < 0);

    cctemp1 = cc;

    printf("cc: %lli \n", cc);

    //length of cc number
    cctemp1 = cc;

    count = 0;
    while(cctemp1 != 0)
    {
        cctemp1 = cctemp1/10;
        count++;
    }
    printf("length: %i \n", count);

    //every other number from second to last.
    
    long long int temp2 = cc/10;
    a = 0;
    int sum = 0;
    
    do
    {
        temp2 = cc / 100;
        a = temp2 % 10;
        sum = sum + a;
    }
    while(temp2 >= 0);
    printf("%i \n", a);

}
