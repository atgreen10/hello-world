#include <stdio.h>
#include <cs50.h>
#include <string.h>
#include <stdlib.h>
#include <ctype.h>

int main(int argc, string argv[])
{
    //get second argument
    if (argc != 2)
    {
        printf("Ya done did somethin' wrong");
        return 1;
    }
    //set parameters for key
    string k = argv[1];
    int klength = strlen(k);

    if(isalpha(k))
    {
        printf("key length: %s \n", k);
    }

    int a;
    int b;
    for (int i = 0; i < klength; i++)
    {
        if(isupper(k))
        {
            //alpha index for key
            a = (k[i] - 'A');
        }
        if(islower(k))
        {
            b = (k[i] - 'a');
        }

    }
}
